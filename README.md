# Analyst OS – Sketch to Insight
A Streamlit website for analysts: translate data dialects, perform temporal fusion joins, run scenario experiments.

## Quick start
```bash
pip install -r requirements.txt
streamlit run app/main.py
